# MES 制造执行系统 — 架构设计与功能点说明

> 文档版本：1.0  
> 更新日期：2026-04-14

---

## 目录

1. [系统概述](#1-系统概述)
2. [技术架构](#2-技术架构)
3. [部署架构](#3-部署架构)
4. [业务架构全景](#4-业务架构全景)
5. [功能模块详解](#5-功能模块详解)
   - 5.1 [系统管理模块](#51-系统管理模块)
   - 5.2 [基础数据模块](#52-基础数据模块)
   - 5.3 [班组管理模块](#53-班组管理模块)
   - 5.4 [工艺管理模块](#54-工艺管理模块)
   - 5.5 [计划管理模块](#55-计划管理模块)
   - 5.6 [工单管理模块](#56-工单管理模块)
   - 5.7 [派工管理模块](#57-派工管理模块)
   - 5.8 [异常管理模块](#58-异常管理模块)
   - 5.9 [质量管理模块](#59-质量管理模块)
   - 5.10 [物料管理模块](#510-物料管理模块)
   - 5.11 [生产查询模块](#511-生产查询模块)
   - 5.12 [APS 集成模块](#512-aps-集成模块)
6. [核心业务流程](#6-核心业务流程)
7. [数据库设计概要](#7-数据库设计概要)
8. [安全与权限体系](#8-安全与权限体系)
9. [测试覆盖说明](#9-测试覆盖说明)
10. [已知风险与改进建议](#10-已知风险与改进建议)

---

## 1. 系统概述

本系统是一套面向离散制造业的 **MES（制造执行系统）**，用于连接企业计划层（ERP/APS）与车间执行层，实现从**订单接收→生产计划→工单下发→派工执行→质量管控→物料追踪→数据反馈**的全链路数字化管理。

### 1.1 核心业务价值

| 价值维度 | 说明 |
|----------|------|
| **计划协同** | 对接 APS 排程系统，实现订单计划→生产计划→工单的三级计划体系 |
| **车间透明化** | 工单状态实时跟踪、派工任务可视化、工作状态一览 |
| **质量闭环** | 开工检查、复检申请、交接班记录，质量问题全程可追溯 |
| **物料精细化** | 库存实时更新、领料/退料/入库全链路管控 |
| **异常快速响应** | 异常联络单流转，影响排程时自动触发 APS 重排 |
| **多租户隔离** | 支持多工厂/租户的数据隔离 |

### 1.2 用户角色

| 角色 | 入口 | 使用场景 |
|------|------|----------|
| **系统管理员** | 管理端（PC 浏览器） | 用户管理、角色权限、系统配置 |
| **计划员** | 管理端 | 订单计划、生产计划管理 |
| **调度员** | 管理端 | 工单管理、派工分配 |
| **质检员** | 管理端 | 质量检验、复检管理 |
| **仓管员** | 管理端 | 库存、领料、入库管理 |
| **现场操作工** | 现场端（STAFF 账号） | 查看工单、派工任务、工作状态 |

---

## 2. 技术架构

### 2.1 技术栈总览

| 层次 | 技术选型 |
|------|----------|
| **前端** | Vue 3 + TypeScript + Vite 5 + Element Plus + Pinia + Vue Router 4 |
| **后端** | Java 17 + Spring Boot 3.2.5 + MyBatis-Plus 3.5.5 |
| **微服务** | Spring Cloud 2023.0.1 + Spring Cloud Alibaba（Nacos） |
| **网关** | Spring Cloud Gateway |
| **数据库** | MySQL 8.0 |
| **缓存** | Redis 7 |
| **消息队列** | RabbitMQ（微服务模式） |
| **认证** | JWT（jjwt）+ Spring Security |
| **API文档** | Knife4j 4.3.0（OpenAPI 3） |
| **工具库** | Hutool、MapStruct、EasyExcel |
| **容器化** | Docker + Docker Compose |
| **监控** | Prometheus + Grafana（微服务模式） |

### 2.2 后端模块划分

```
mes-backend/
├── mes-common           # 公共组件：统一响应、异常处理、工具类、事件、Feign 客户端
├── mes-framework        # 框架层：Security、JWT、Redis、MyBatis-Plus 配置
├── mes-system           # 系统管理：用户、角色、菜单、认证
├── mes-basic            # 基础数据：物料、物料价格、工作中心
├── mes-team             # 班组管理：生产班组
├── mes-process          # 工艺管理：工艺信息、模板、指导书、制造BOM
├── mes-plan             # 计划管理：订单计划、生产计划
├── mes-workorder        # 工单管理：生产工单及子表
├── mes-dispatch         # 派工管理：派工任务、分配
├── mes-abnormal         # 异常管理：异常联络单
├── mes-quality          # 质量管理：复检、开工检查、交接班
├── mes-material         # 物料管理：库存、领料、退料、入库
├── mes-query            # 生产查询：工作状态、生产/检验查询
├── mes-aps              # APS集成：同步、回调、主数据推送
├── mes-admin            # 单体聚合启动器（依赖以上所有模块）
├── mes-gateway          # API 网关（微服务模式）
└── mes-*-service        # 微服务启动器（按领域拆分）
```

### 2.3 前端模块划分

```
mes-frontend/src/
├── api/                 # API 接口封装（32 个文件，按业务域分子目录）
├── views/               # 页面组件（44 个 .vue 文件）
├── router/              # 路由配置（主路由 + modules/ 子路由）
├── stores/              # Pinia 状态管理（auth、permission）
├── layout/              # 布局组件（MainLayout、UserPortalLayout、侧栏、面包屑）
├── components/          # 通用组件（SearchForm、DataTable、BatchEdit）
├── utils/               # 工具函数（Axios 封装、字典）
└── types/               # TypeScript 类型定义
```

---

## 3. 部署架构

系统支持两种部署模式：

### 3.1 单体模式（docker-compose.yml）

适用于中小规模部署，所有后端模块打包在 `mes-admin` 中作为单个 JVM 运行。

```
┌─────────────────────────────────────────────────┐
│                  Docker 主机                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────────┐   │
│  │  MySQL   │  │  Redis   │  │  mes-backend │   │
│  │  :3307   │  │  :6379   │  │  :9090       │   │
│  └──────────┘  └──────────┘  └──────────────┘   │
│                                                   │
│  ┌──────────────────────────────────────────┐    │
│  │  mes-frontend (Nginx)  :80               │    │
│  └──────────────────────────────────────────┘    │
└─────────────────────────────────────────────────┘
```

- 后端端口：9090（prod）/ 9091（dev）
- 前端 Nginx 代理 `/api` 到后端
- 适合开发调试和单机部署

### 3.2 微服务模式（docker-compose.microservice.yml）

适用于大规模生产环境，服务按领域拆分。

```
┌─────────────────────────────────────────────────────────────┐
│                       Docker 主机                             │
│  ┌────────┐ ┌───────┐ ┌──────────┐ ┌───────┐                │
│  │ MySQL  │ │ Redis │ │ RabbitMQ │ │ Nacos │                │
│  └────────┘ └───────┘ └──────────┘ └───────┘                │
│                                                               │
│  ┌──────────────────── mes-gateway :8080 ─────────────────┐  │
│  │  /api/auth/**  → mes-system-service      :9001         │  │
│  │  /api/basic/** → mes-master-data-service :9002         │  │
│  │  /api/plan/**  → mes-production-service  :9003         │  │
│  │  /api/quality/**→ mes-quality-service    :9004         │  │
│  │  /api/material/**→ mes-material-service  :9005         │  │
│  │  /api/aps/**   → mes-integration-service :9006         │  │
│  │  /api/query/** → mes-query-service       :9007         │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                               │
│  ┌──── 监控 ────┐                                             │
│  │ Prometheus   │  ┌──────────────────────────────┐          │
│  │ Grafana      │  │  mes-frontend (Nginx) :80    │          │
│  └──────────────┘  └──────────────────────────────┘          │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. 业务架构全景

```
                    ┌─────────────────┐
                    │   ERP / APS     │
                    │  (外部系统)      │
                    └────────┬────────┘
                             │ REST API
                    ┌────────▼────────┐
                    │   APS 集成模块   │ ← 上行同步 / 下行回调
                    └────────┬────────┘
                             │
    ┌────────────────────────┼────────────────────────┐
    │                        │                        │
┌───▼──────┐   ┌─────────────▼──────────┐   ┌────────▼────────┐
│ 计划管理  │──▶│      工单管理          │──▶│    派工管理      │
│ 订单计划  │   │ 生产工单（含子表）      │   │ 任务生成/分配   │
│ 生产计划  │   │ 状态：创建→下发→       │   │ 人员/设备/班组  │
└──────────┘   │   开工→完工            │   └─────────────────┘
               └────────────┬───────────┘
                            │
         ┌──────────────────┼──────────────────┐
         │                  │                  │
    ┌────▼─────┐    ┌───────▼──────┐    ┌──────▼──────┐
    │ 质量管理  │    │  异常管理    │    │  物料管理    │
    │ 开工检查  │    │  异常联络单  │    │  库存/领料   │
    │ 复检申请  │    │  草稿→提交→  │    │  退料/入库   │
    │ 交接班    │    │  处理→关闭   │    │  要货/签收   │
    └──────────┘    └──────────────┘    └─────────────┘
         │                  │                  │
         └──────────────────┼──────────────────┘
                            │
                    ┌───────▼───────┐
                    │   基础数据     │
                    │ 物料/工作中心  │
                    │ 工艺/BOM/班组  │
                    └───────────────┘
```

---

## 5. 功能模块详解

### 5.1 系统管理模块

**定位**：系统基础支撑，提供用户认证、权限控制、多租户管理。

#### 5.1.1 用户认证

| 功能 | 业务规则 |
|------|----------|
| **登录** | 支持管理端（ADMIN）和现场端（USER）两个入口；STAFF 类型账号**仅能**从现场端登录，管理端会拒绝 |
| **Token 管理** | JWT 双 Token 机制：accessToken（短效）+ refreshToken（长效）；刷新时校验 tokenType 必须为 `refresh` |
| **权限缓存** | 登录成功后将用户权限写入 Redis Set，key 为 `auth:permissions:{userId}`，过期 2 小时 |
| **登出** | 清除 Redis 权限缓存；Redis 不可用时降级处理（不抛异常） |

#### 5.1.2 用户管理

- 用户名全局唯一
- 创建时默认密码 `123456`
- 账号类型：`ADMIN`（管理端）、`STAFF`（现场端）
- **多租户隔离**：列表查询自动按 `tenantId` 过滤，增删改检查 `assertSameTenant`

#### 5.1.3 角色与权限

- 角色编码唯一
- 菜单分配：先清空再批量插入（全量替换）
- 菜单树：`STAFF` 账号自动过滤掉 `/system` 路径下的管理菜单

#### 5.1.4 前端权限控制

- **路由级**：无 Token 重定向登录页；STAFF 仅可访问 `/app/*` 现场端
- **菜单级**：优先使用后端动态菜单，无数据时回退静态菜单配置
- **按钮级**：后端已定义 `permission` 字段，前端预留扩展

---

### 5.2 基础数据模块

**定位**：维护生产所需的主数据，为工艺、计划、工单等模块提供数据基础。

#### 5.2.1 物料档案

| 功能 | 业务规则 |
|------|----------|
| **编码唯一** | `materialCode` 创建和更新时均校验唯一性 |
| **追溯方式联动** | `SERIAL` 追溯必须配置序列号生成器；`BATCH` 追溯必须配置批号生成器 |
| **删除保护** | 存在物料价格引用时禁止删除 |
| **缓存机制** | 详情查询启用 `@Cacheable`，更新/删除 `@CacheEvict` 清除缓存 |
| **支持字段** | 编码、名称、类型、图号、规格型号、产品类别、机型、基本单位、追溯方式等 |

#### 5.2.2 物料价格

- **一物一价**：同一物料仅允许一条价格记录
- 创建/更新前校验物料存在性

#### 5.2.3 工作中心

- 编码唯一
- 支持批量更新（`batchUpdate`），逐条校验
- 关联工艺工序（`ProcessInfo.workCenterId`）

---

### 5.3 班组管理模块

**定位**：管理生产车间的班组信息，为派工分配提供班组数据。

| 功能 | 说明 |
|------|------|
| CRUD | 标准增删改查 |
| 启用/停用 | `toggle-enabled` 切换班组可用状态 |
| 字段 | 班组编码、名称、所属组织编码/名称、启用状态 |

---

### 5.4 工艺管理模块

**定位**：管理产品的制造工艺信息，包括工艺路线、工序模板、制造BOM。

#### 5.4.1 工序模板

- **树形结构**：通过 `parentProcessNo` 构建父子关系
- **唯一校验**：`processNo` 唯一
- **删除保护**：存在子工序引用时禁止删除
- **防止自引用**：父工序不能指向自身

#### 5.4.2 工艺信息（ProcessInfo）

- 标准 CRUD + 批量更新
- 关联工作中心（`workCenterId`）
- 包含加工时间、拆卸时间、安装时间等生产参数

#### 5.4.3 制造 BOM

这是工艺模块最核心的功能，具有完整的版本管理和状态控制：

| 操作 | 前置条件 | 结果 |
|------|----------|------|
| **创建** | - | 初始版本 V1，状态 DRAFT |
| **编辑** | 仅 DRAFT | 先删明细再递归插入 |
| **升级** | - | 同 bomCode 新版本号（Vn+1），深拷贝全部数据，新 BOM 为 DRAFT |
| **发布** | 仅 DRAFT | DRAFT → PUBLISHED；同 bomCode 下其他 PUBLISHED 自动改为 DISABLED |
| **停用** | 仅 PUBLISHED | PUBLISHED → DISABLED |

- **明细树**：支持递归子节点 + 替代料列表
- **版本日志**：每次操作记录 `BomVersionLog`

---

### 5.5 计划管理模块

**定位**：管理从客户订单到生产执行的计划体系，是连接 ERP 与车间的桥梁。

#### 5.5.1 订单计划

订单计划具有**四维状态管理**：

| 状态维度 | 枚举 | 说明 |
|----------|------|------|
| **主状态** | CREATED → RELEASED → COMPLETED / TERMINATED | 计划生命周期 |
| **流程状态** | RUNNING / COMPLETED / TERMINATED | 计划流转状态 |
| **展开状态** | UNEXPANDED / EXPANDED | 是否已展开为生产计划 |
| **完成状态** | NOT_STARTED / APPROVED | 完成审批状态 |

**状态流转规则**：

```
CREATED ──(下达)──▶ RELEASED ──(完成)──▶ COMPLETED
                        │                    │ flowStatus = COMPLETED
                        │                    │ completionStatus = APPROVED
                        │
                        ├──(终止)──▶ TERMINATED
                        │             flowStatus = TERMINATED
                        │
                        └──(展开)──▶ expandStatus = EXPANDED
                                    （须 RELEASED + UNEXPANDED）
```

- 编辑/删除：仅 CREATED 状态允许
- 数据来源：默认 `MANUAL`（手工），可对接 ERP 为 `ERP`

#### 5.5.2 生产计划

| 功能 | 业务规则 |
|------|----------|
| **创建** | 必须关联已下达（RELEASED）的订单计划；`planQty > 0` |
| **下达** | CREATED → RELEASED；**自动创建工单**（工单号：`WO-yyyyMMdd-xxxx`） |
| **编辑/删除** | 仅 CREATED 状态 |

**关键联动**：生产计划下达时自动触发工单创建，是计划到执行的核心衔接点。

---

### 5.6 工单管理模块

**定位**：生产执行的核心载体，管理工单的完整生命周期及其子表数据。

#### 5.6.1 工单主表

| 状态 | 代码 | 可执行操作 |
|------|------|------------|
| 已创建 | CREATED | 编辑、删除、下发 |
| 已下发 | RELEASED | 开工 |
| 执行中 | IN_PROGRESS | 完工、强制完工 |
| 已完工 | COMPLETED | （终态） |
| 强制完工 | FORCE_COMPLETED | （终态，需填原因） |

**状态流转**：

```
CREATED ──(下发)──▶ RELEASED ──(开工)──▶ IN_PROGRESS ──(完工)──▶ COMPLETED
                                              │
                                              └──(强制完工)──▶ FORCE_COMPLETED
                                                   ↑ 必须填写原因
```

**下发校验**：必须包含至少一个工作清单（`WorkOrderTask`），否则拒绝下发。

#### 5.6.2 工单子表（7 张）

| 子表 | 说明 |
|------|------|
| **工作清单**（WorkOrderTask） | 工单的工序任务列表，按 sequenceNo 排序；初始状态 CREATED |
| **投料清单**（InputMaterial） | 生产所需物料清单；`issuedQty` 初始为 0，随领料更新 |
| **产出清单**（OutputMaterial） | 预期产出物料 |
| **质检项**（QualityItem） | 需检验的质量项；初始状态 PENDING |
| **约束关系**（Constraint） | 工单间的先后依赖关系（如 FINISH_TO_START） |
| **供应计划**（SupplyPlan） | 物料供应计划；`completedQty` 初始为 0 |
| **附件**（Attachment） | 工单相关附件文件 |

#### 5.6.3 事件驱动

工单每次状态变更都会：
1. 记录状态日志（`WorkOrderStatusLog`）
2. 发布 **APS 同步事件**（`ApsSyncEvent`），通知 APS 系统
3. 下发时额外发布 **`WorkOrderReleasedEvent`**，触发派工任务自动生成

---

### 5.7 派工管理模块

**定位**：将工单任务分配到具体的人员、设备或班组。

#### 5.7.1 派工任务生成

- **触发方式**：监听 `WorkOrderReleasedEvent`，工单下发后**自动生成**派工任务
- **生成规则**：一个工单任务（WorkOrderTask）对应一个派工任务（DispatchTask）
- **防重复**：同一工单已存在派工任务则拒绝重复生成
- **初始状态**：UNASSIGNED（未分派）

#### 5.7.2 分配管理

支持三种分配类型：

| 类型 | 接口 | 说明 |
|------|------|------|
| **人员分配** | `assignPerson` | 分配给具体操作工 |
| **设备分配** | `assignDevice` | 分配到具体设备 |
| **班组分配** | `assignTeam` | 分配给生产班组 |

**分配规则**：
- 仅 `UNASSIGNED` 或 `REVOKED` 状态的任务可派工
- 分配后任务状态变为 `ASSIGNED`
- 一个任务可有多条分配记录

#### 5.7.3 撤销分配

- 仅 `ACTIVE` 状态的分配可撤销
- 撤销后若**无其他有效分配**，任务状态回退为 `UNASSIGNED`
- 若**仍有其他有效分配**，任务保持 `ASSIGNED`

---

### 5.8 异常管理模块

**定位**：处理生产过程中的异常事件，支持与 APS 联动自动重排。

#### 5.8.1 异常联络单状态流转

```
DRAFT ──(提交)──▶ SUBMITTED ──(处理)──▶ PROCESSING ──(关闭)──▶ CLOSED
```

| 操作 | 前置状态 | 业务规则 |
|------|----------|----------|
| 创建 | - | 自动生成单号（YC-前缀）或手工填写（须唯一） |
| 编辑 | DRAFT | 单号不允许修改 |
| 删除 | DRAFT | 同时清理附件和操作日志 |
| 提交 | DRAFT | 记录发布时间；若影响排程则触发 APS 重排 |
| 处理 | SUBMITTED | 进入处理阶段 |
| 关闭 | PROCESSING | 必须先经过"处理"才能关闭 |

#### 5.8.2 APS 重排联动

当异常联络单满足以下条件时触发 APS 重排：
1. `affectSchedule = 1`（标记影响排程）
2. **5 分钟防抖**：同一事件分类（`eventCategory`）在 5 分钟内不会重复触发

#### 5.8.3 附件管理

| 操作 | 规则 |
|------|------|
| 添加 | 新附件默认 `signed = 0`（未签署） |
| 删除 | 已签署的附件**不可删除** |
| 签署 | 已签署的附件**不可重复签署** |

---

### 5.9 质量管理模块

**定位**：管理生产过程中的质量检验和质量控制。

#### 5.9.1 功能清单

| 功能 | 说明 |
|------|------|
| **订单开工检查** | 在订单开始生产前进行质量前置检查 |
| **工作开工检查** | 在具体工序开始前进行质量确认 |
| **复检申请** | 质量问题需要复检时提交申请；创建时发布 APS 同步事件 |
| **交接班记录** | 班组交接时记录计划数量、实际数量、差异分析 |

#### 5.9.2 复检申请状态

- 创建后状态为 `CREATED`
- 支持关联订单计划、序列号子表
- 创建时自动发布 `ApsSyncEvent("QUALITY","RECHECK",...)`

---

### 5.10 物料管理模块

**定位**：管理物料的全生命周期流转，覆盖库存、领料、退料、入库。

#### 5.10.1 库存管理

| 功能 | 业务规则 |
|------|----------|
| **创建** | null 库存量自动初始化为 0（非限制/质检/冻结三个维度） |
| **扣减** | 调用 Mapper 层扣减，失败断言"库存不足"；成功后发布 APS 同步事件 |
| **增加** | 直接增加库存并发布 APS 同步事件 |

库存分为三个维度：
- **非限制库存**（unrestrictedStock）：可自由使用
- **质检库存**（qualityStock）：待质检
- **冻结库存**（frozenStock）：被冻结不可用

#### 5.10.2 领料管理

| 功能 | 业务规则 |
|------|----------|
| **创建领料单** | 单号自动生成（LL-前缀）；创建时即按明细扣减库存 |
| **删除领料单** | 按明细回补库存 |
| **注意** | 更新领料单时**未处理库存差异回滚**（已知待优化点） |

#### 5.10.3 退料管理

- 单号自动生成（TL-前缀）
- 创建退料单时按物料编码找库存并增加
- **注意**：删除退料单时**未做库存回补**（与领料/入库不对称）

#### 5.10.4 完工入库

- 单号自动生成（RK-前缀）
- 创建时按明细增加库存
- 删除时按明细扣减库存（与创建对称）

---

### 5.11 生产查询模块

**定位**：提供面向管理层和现场的综合查询视图。

| 查询功能 | 说明 |
|----------|------|
| **生产工作查询** | 查询当前生产进度和工作状态 |
| **检验工作查询** | 查询质量检验相关工作 |
| **工作状态视图** | 聚合展示各工位/工序的实时状态 |
| **工单查询** | 按条件查询工单信息 |
| **派工查询** | 查询派工分配情况 |
| **交接班附件查询** | 查询交接班相关附件 |
| **开工检查查询** | 查询开工检查记录 |

---

### 5.12 APS 集成模块

**定位**：实现 MES 与 APS（高级计划排程）系统的双向数据同步。

#### 5.12.1 上行同步（MES → APS）

| 功能 | 说明 |
|------|------|
| **事件驱动** | 工单状态变更、库存变动、质量异常等通过 `ApsSyncEvent` 触发 |
| **队列机制** | 事件入队（`ApsSyncQueue`），状态 PENDING；定时批量推送到 APS |
| **重试策略** | 指数退避重试，超过最大重试次数标记 FAILED |
| **主数据同步** | 支持工作中心、工艺路线、BOM、物料、班组的全量/增量同步 |

#### 5.12.2 下行回调（APS → MES）

| 回调类型 | 处理逻辑 |
|----------|----------|
| **排程结果** | 按 apsOrderId 匹配订单计划，更新工单计划起止时间 |
| **MRP 结果** | 接收物料需求计划 |
| **资源分配** | 自动更新派工任务时间，创建分配记录（assignedBy = "APS"） |
| **甘特图数据** | 接收排程甘特图数据 |
| **产能负荷** | 接收工作中心负荷率数据（含超负荷预警） |
| **排程变更** | 接收排程变更通知，自动更新工单计划时间 |

#### 5.12.3 安全

- APS 回调接口（`/aps/callback/**`）配置为**匿名访问**，不需要 JWT 认证
- 通过 API Key 进行身份验证（`X-API-Key` Header）

---

## 6. 核心业务流程

### 6.1 从订单到交付的主流程

```
1. 接收客户订单
   └─▶ 创建【订单计划】（CREATED）
        └─▶ 下达订单计划（RELEASED）
             └─▶ 展开为【生产计划】
                  └─▶ 下达生产计划 ──自动创建──▶ 【工单】（CREATED）
                       └─▶ 下发工单（RELEASED）──自动生成──▶ 【派工任务】
                            └─▶ 分配人员/设备/班组
                                 └─▶ 开工（IN_PROGRESS）
                                      ├─▶ 领料（扣减库存）
                                      ├─▶ 开工检查
                                      ├─▶ 生产执行
                                      ├─▶ 交接班
                                      └─▶ 完工（COMPLETED）
                                           └─▶ 成品入库（增加库存）
```

### 6.2 异常处理流程

```
发现异常
└─▶ 创建【异常联络单】（DRAFT）
     └─▶ 提交（SUBMITTED）
          ├─▶ 影响排程？──是──▶ 触发 APS 重排（5分钟防抖）
          └─▶ 开始处理（PROCESSING）
               └─▶ 关闭（CLOSED）
```

### 6.3 APS 交互流程

```
MES 侧事件（工单/库存/质量/异常）
└─▶ ApsSyncEvent
     └─▶ ApsSyncEventListener
          ├─▶ 入队（PENDING）──定时──▶ HTTP 推送 APS
          └─▶ 执行反馈（DISPATCH/STATUS_CHANGE 等）

APS 侧排程完成
└─▶ POST /aps/callback/schedule-result
     └─▶ 更新工单计划时间
          └─▶ 更新工单任务分段（WorkOrderTaskSegment）
```

---

## 7. 数据库设计概要

### 7.1 表清单

数据库使用 MySQL 8.0，通过 Flyway 风格迁移脚本管理（V1.00 ~ V1.16），共约 **60+ 张业务表**。

| 版本 | 模块 | 主要表 |
|------|------|--------|
| V1.00 | 基础数据 | `mes_material`、`mes_material_price`、`mes_work_center` |
| V1.01 | 班组 | `mes_production_team` |
| V1.02 | 工艺 | `mes_instruction`、`mes_process_*`、`mes_manufacturing_bom*` 等 14 张 |
| V1.03 | 计划 | `mes_order_plan`、`mes_production_plan`、`mes_plan_status_log` |
| V1.04 | 工单 | `mes_work_order` 及 7 张子表 + 状态日志 |
| V1.05 | 派工 | `mes_dispatch_task`、`mes_dispatch_assignment`、状态日志 |
| V1.06 | 异常 | `mes_abnormal_contact`、附件、日志 |
| V1.07 | 质量 | 复检、开工检查、交接班等 6 张 |
| V1.08 | 查询 | 工作查询相关表和视图 |
| V1.09 | 物料 | 库存、领料、入库、退料、签收等 9 张 |
| V1.10 | APS | 同步配置/日志/队列、数据映射、任务分段等 |
| V1.11 | 权限 | `sys_user`、`sys_role`、`sys_menu` 等 5 张 + 种子数据 |
| V1.14 | 租户 | `sys_tenant`；用户表增加租户字段 |
| V1.16 | 多租户 | 所有业务表添加 `tenant_id` 字段和索引 |

### 7.2 设计规范

- **引擎**：InnoDB + utf8mb4
- **公共字段**：`created_by`、`created_time`、`updated_by`、`updated_time`、`deleted`
- **逻辑删除**：`deleted` 字段（TINYINT），MyBatis-Plus 全局配置
- **多租户**：`tenant_id` 字段实现数据隔离

---

## 8. 安全与权限体系

### 8.1 认证流程

```
用户登录
└─▶ AuthenticationManager 校验用户名密码
     └─▶ UserDetailsServiceImpl 加载用户信息
          └─▶ JwtTokenProvider 签发 accessToken + refreshToken
               └─▶ 权限写入 Redis（2小时过期）
```

### 8.2 请求鉴权

```
请求到达
└─▶ JwtAuthenticationFilter
     └─▶ 解析 Bearer Token
          └─▶ 仅 accessToken 类型建立安全上下文
               └─▶ 从 Redis 加载权限集合
                    └─▶ StaffPortalRestrictionFilter
                         └─▶ STAFF 禁止访问 /system/**
                              └─▶ TenantContextHolder 设置租户上下文
```

### 8.3 公开接口（无需认证）

- `/auth/login` — 登录
- `/auth/refresh` — 刷新 Token
- `/aps/callback/**` — APS 回调
- `/files/**` — 静态文件
- Knife4j 文档相关路径

### 8.4 安全增强

- **限流**：`RateLimitFilter` 防止接口滥用
- **无状态**：SessionCreationPolicy.STATELESS
- **CORS**：支持跨域（前后端分离）
- **Redis 降级**：权限缓存 Redis 不可用时空权限降级运行

---

## 9. 测试覆盖说明

### 9.1 测试文件清单

| 测试文件 | 模块 | 测试用例数 | 覆盖范围 |
|----------|------|------------|----------|
| `SystemModuleTest.java` | 系统管理 | 11 | 登录认证、Token 刷新、登出、用户信息 |
| `BasicModuleTest.java` | 基础数据 | 10 | 物料 CRUD、编码唯一、追溯方式校验、删除保护 |
| `PlanModuleTest.java` | 计划管理 | 16 | 订单计划全生命周期、四维状态管理、枚举完整性 |
| `WorkOrderModuleTest.java` | 工单管理 | 16 | 工单 CRUD、完整状态流转、子表管理、事件发布 |
| `DispatchModuleTest.java` | 派工管理 | 12 | 任务生成、三种分配类型、撤销分配、防重复 |
| `AbnormalModuleTest.java` | 异常管理 | 15 | 状态流转、附件签署、APS 防抖、单号生成 |
| `MaterialMgmtModuleTest.java` | 物料管理 | 9 | 库存 CRUD、扣减/增加、APS 事件 |
| `ApsIntegrationFullTest.java` | APS 集成 | 20 | 主数据同步、执行反馈、回调处理、枚举完整性 |
| **合计** | | **109** | |

### 9.2 测试策略

- **技术栈**：JUnit 5 + Mockito
- **测试类型**：单元测试（Mock 依赖层）
- **覆盖重点**：
  - 状态机正向流转与非法流转
  - 数据校验规则（唯一性、必填、联动）
  - 边界条件（null、0、负数、空集合）
  - 模块间事件联动
  - 枚举完整性

---

## 10. 已知风险与改进建议

### 10.1 数据一致性风险

| 风险点 | 描述 | 建议 |
|--------|------|------|
| **领料单更新** | 更新领料单时未处理库存差异回滚 | 更新时先回补旧数量再扣减新数量 |
| **退料单删除** | 删除退料单时未回补库存 | 保持与领料单/入库单的删除对称性 |
| **领料库存匹配** | 领料按 `materialId` 取第一条库存（LIMIT 1），未按仓库/库位匹配 | 增加仓库/库位维度的精确匹配 |

### 10.2 并发风险

| 风险点 | 描述 | 建议 |
|--------|------|------|
| **工单号生成** | 生产计划下达时工单号用随机数生成（`WO-yyyyMMdd-xxxx`） | 使用分布式 ID 或数据库序列 |
| **APS 防抖** | 异常联络单的防抖使用内存 `ConcurrentHashMap`，多实例部署会失效 | 迁移到 Redis 分布式防抖 |

### 10.3 功能完善建议

| 建议 | 说明 |
|------|------|
| **物料删除引用检查** | 工序/BOM/工单引用检查标记为 TODO，建议补全 |
| **操作人记录** | 多处状态日志 `operator` 写死 `"system"`，应从 SecurityContext 获取 |
| **工作中心删除保护** | 删除检查标记为 TODO，需验证工序/工单引用 |
| **按钮级权限** | 后端 `sys_menu.permission` 已定义，前端需补充 `v-permission` 指令 |
| **集成测试** | 当前仅有单元测试，建议补充 Spring Boot 集成测试和 E2E 测试 |
